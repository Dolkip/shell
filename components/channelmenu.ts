import { BoxRenderable, TextRenderable, SelectRenderable, ScrollBoxRenderable } from "@opentui/core";
import { renderer } from "../renderer";
import { Theme } from "../theme";

const serverChannels = new SelectRenderable(renderer, {
    
})